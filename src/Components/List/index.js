import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export default function List(props) {
 return (
   <View>
    <Text>{data.props.name}</Text>
    <Image/>
   </View>
  );
}

const styles = StyleSheet.create({

})